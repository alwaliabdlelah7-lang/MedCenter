# MedCenter-HIS Environment Configuration

# Application Settings
VITE_APP_NAME="نظام إيداع الطبي"
VITE_APP_VERSION="1.0.0"

# API Endpoints (If integrated with backend)
VITE_API_BASE_URL=

# AI Features (Optional)
GEMINI_API_KEY=

# Security
# In this version, we use localStorage for persistence. 
# In production, replace with secure backend session management.
